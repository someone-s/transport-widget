# Privacy declaration

For certain transport data source, API keys are required for access.

API keys are only shared with the data source they relate to, so that data can be retreived from said data source.

API keys are stored on device and not shared with any other parties except the data source they relate to.